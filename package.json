{
  "name": "agent-forge-engineering-dossier",
  "version": "0.2.0",
  "private": true,
  "description": "Public engineering dossier for Agent Forge, linking to the existing website and product tour.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:boundary": "node scripts/check-public-boundary.mjs",
    "check:links": "node scripts/check-links.mjs",
    "build": "node scripts/build-dossier.mjs",
    "check": "npm run test && npm run check:boundary && npm run check:links && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
