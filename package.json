{
  "name": "agent-forge-showcase",
  "version": "0.1.0",
  "private": true,
  "description": "A synthetic, public-safe product tour of the Agent Forge orchestration system.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:boundary": "node scripts/check-public-boundary.mjs",
    "check:links": "node scripts/check-links.mjs",
    "build": "node scripts/build-pages.mjs",
    "check": "npm run test && npm run check:boundary && npm run check:links && npm run build",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
