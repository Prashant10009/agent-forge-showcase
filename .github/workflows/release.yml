name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: npm ci
      - run: npm run check
      - run: zip -r agent-forge-dossier-${{ github.ref_name }}.zip dist
      - run: sha256sum agent-forge-dossier-${{ github.ref_name }}.zip > agent-forge-dossier-${{ github.ref_name }}.zip.sha256
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${GITHUB_REF_NAME}" "agent-forge-dossier-${GITHUB_REF_NAME}.zip" "agent-forge-dossier-${GITHUB_REF_NAME}.zip.sha256" --generate-notes --title "Agent Forge Engineering Dossier ${GITHUB_REF_NAME}"
